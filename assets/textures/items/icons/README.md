# Item Icons

Small square textures (64×64 or 128×128) shown in the 10-slot item bar, equipment panels, and shop UI.

Filename convention: `[item_id].png` (matches the corresponding `.tres` in `res://assets/items/`).

See parent `res://assets/textures/items/README.md` for import settings.
